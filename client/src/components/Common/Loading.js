import React from "react";
import {Box, Typography} from "@mui/material";

const Loading = () => {
    return(
        <Box sx={{margin: '100px'}}>
            <Typography variant="h2" sx={{textAlign: "center"}}>Loading...</Typography>
        </Box>
    )
}

export default Loading;